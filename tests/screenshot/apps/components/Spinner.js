import Spinner from '../../../../Spinner';
import React from 'react';

const SpinnerTests = [
	<Spinner />,
	<Spinner centered />,
	<Spinner size="huge" />,
	<Spinner type="loading" />
];

export default SpinnerTests;
